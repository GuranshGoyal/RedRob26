# Core data / ML
numpy>=1.24,<2.0
pandas>=2.0
scikit-learn>=1.3

# Text retrieval
rank-bm25>=0.2.2

# Dense embeddings / cross-encoder (CPU-only, no GPU required)
sentence-transformers>=2.2
faiss-cpu>=1.7.4

# Sandbox (optional for local reproduction)
streamlit>=1.28
